server.port=${PORT:8080}
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/MarketPilot}
spring.datasource.username=${DATABASE_USERNAME:pragadeeswaran}
spring.datasource.password=${DATABASE_PASSWORD:pragadees}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# AI engine (Python FastAPI)
ai.engine.url=${AI_ENGINE_URL:http://localhost:8000}
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
